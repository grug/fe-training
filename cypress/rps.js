context('Rock/Paper/Scissors', () => {
  it('Lets the user select a weapon', () => {

  });

  it('Displays what the computer has selected after the user has selected a weapon', () => {

  });

  it('Displays results for a game', () =>{

  });

  it('Hides the computer choice and results when the reset button is hit', () => {

  });
});
